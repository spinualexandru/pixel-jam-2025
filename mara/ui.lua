local typography = require("mara.ui.typography")
local button = require("mara.ui.button")
local colors = require("mara.ui.colors")
local checkbox = require("mara.ui.checkbox")
local slider = require("mara.ui.slider")
local dropdown = require("mara.ui.dropdown")

local ui = {
    typography = typography,
    button = button,
    colors = colors,
    checkbox = checkbox,
    slider = slider,
    dropdown = dropdown
}

return ui
